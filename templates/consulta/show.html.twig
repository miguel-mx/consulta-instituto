{% extends 'base.html.twig' %}

{% block title %}Consulta{% endblock %}

{% block body %}
    <h1>Consulta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consultum.id }}</td>
            </tr>
            <tr>
                <th>Tareas</th>
                <td>{{ consultum.tareas ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Comentario1</th>
                <td>{{ consultum.comentario1 }}</td>
            </tr>
            <tr>
                <th>Documento</th>
                <td>{{ consultum.documento ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Comentario2</th>
                <td>{{ consultum.comentario2 }}</td>
            </tr>
            <tr>
                <th>Academico</th>
                <td>{{ consultum.academico ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Comentario3</th>
                <td>{{ consultum.comentario3 }}</td>
            </tr>
            <tr>
                <th>Comision</th>
                <td>{{ consultum.comision ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Comentario4</th>
                <td>{{ consultum.comentario4 }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ consultum.fecha ? consultum.fecha|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_consulta_index') }}">back to list</a>

    <a href="{{ path('app_consulta_edit', {'id': consultum.id}) }}">edit</a>

    {{ include('consulta/_delete_form.html.twig') }}
{% endblock %}
